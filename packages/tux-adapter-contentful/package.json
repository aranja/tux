{
  "name": "tux-adapter-contentful",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && BABEL_ENV=production babel src --out-dir lib",
    "watch": "BABEL_ENV=development babel src --watch --out-dir lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.15.3"
  },
  "peerDependencies": {
    "react": "^15.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2"
  }
}
